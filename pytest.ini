[pytest]
markers =
    integration: tests that hit a real database

# Enable live logging to the console during tests
log_cli = true
log_cli_level = INFO
log_format = %(asctime)s %(levelname)s %(name)s: %(message)s
log_date_format = %H:%M:%S


